# Base URL of the Fadhili AI ML backend (FastAPI).
# Leave unset during frontend-only development — the app will
# clearly display "model not connected" states instead of
# fabricating results.
VITE_API_BASE_URL=http://localhost:8000
